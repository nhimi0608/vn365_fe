import { Profile } from "@/components/features/profile";
import React from "react";

const ProfilePage = () => {
  return (
    <div
      style={{
        minHeight: "100vh",
      }}
    >
      <Profile />
    </div>
  );
};

export default ProfilePage;
